DOCKER 

docker build -t <public image name> .
docker run --name <private image name> -p <ports> -d <public image name>

DOCKER SWARM

docker service run --name <private image name> -p <ports> -d <public image name>
docker service scale webserver=2

